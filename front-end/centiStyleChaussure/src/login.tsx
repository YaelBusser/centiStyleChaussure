import { Card, Input } from "@mui/material"

const Login = () => {
    return (
        <Card >
            <Input placeholder='Nom de compte'/>
            <Input placeholder='Mot de passe'/>
        </Card>
    )
}

export default Login